{
  "name": "@speclyy/db",
  "version": "0.0.1",
  "private": true,
  "description": "Speclyy shared database layer — Drizzle schema + Postgres client. Used by every app that reads or writes Supabase-hosted Postgres.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts",
    "./client": "./src/client.ts"
  },
  "files": ["src"],
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0",
    "typescript": "^5"
  }
}
